spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  liquibase:
    enabled: false
    datasource:
      url: jdbc:h2:mem:librarydb # в корне проект файл бд db.mv.db
      username: sa # имя пользователя консоли
      password: 1 # пароль консоли
  main:
    allow-circular-references: true
    #spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
    #initialization-mode: always
    #schema: schema.sql
    #data: data.sql
#  sql:
#    init:
#      mode: always
#      data-locations: data.sql
#      schema-locations: schema.sql
#  main:
#    allow-circular-references: true
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#      settings:
#        web-allow-others: true
#  liquibase:
#    enabled: true